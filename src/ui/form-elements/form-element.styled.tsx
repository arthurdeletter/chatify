import styled from "styled-components";

export const FormElementContainer = styled.div({
  display: "flex",
  flexDirection: "column",
});

export const FormElementDescription = styled.p({
  fontSize: ".8rem",
  color: "rgba(0, 0, 0, .6)",
  marginTop: ".25rem",
});
