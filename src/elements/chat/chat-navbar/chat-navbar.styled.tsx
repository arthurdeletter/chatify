import styled from "styled-components";
import { Flex } from "../../../ui";

export const ChatNavContainer = styled(Flex)({
  padding: "1.5rem",
  h3: {
    fontSize: "1.1rem",
  },
});

export const ActionsContainer = styled(Flex)({});
